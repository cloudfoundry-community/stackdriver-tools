---
name: google-fluentd

packages:
  - google-fluentd

templates:
  google-fluentd-ctl: bin/google-fluentd-ctl
  google-fluentd.conf: config/google-fluentd.conf
  fluentd.conf: config/config.d/fluentd.conf
  syslog.conf: config/config.d/syslog.conf
  syslog_endpoint.conf: config/config.d/syslog_endpoint.conf
  vcap.conf: config/config.d/vcap.conf

properties: {}
